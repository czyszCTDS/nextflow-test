docker {
    enabled = true
}

process {
    executor = 'k8s'
}

k8s {
    storageClaimName = 'nextflow-pvc'
    storageMountPath = '/workflow'
    pullPolicy = 'Always'
    computeResourceType = 'Job'
}